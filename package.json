{
  "name": "pvme-settings-local-tools",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:site": "node scripts/build-site.mjs",
    "refresh:fallback-recognition": "node scripts/generate-fallback-recognition.mjs",
    "dev": "npm run build:site && node scripts/dev.mjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^0.1.80",
    "jsdom": "^30.1.0"
  }
}
